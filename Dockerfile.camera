FROM python:3.9-slim

WORKDIR /app

# Install dependencies
RUN pip install paho-mqtt requests

# Copy script
COPY camera_ingestion.py .

# Create uploads directory
RUN mkdir -p /app/uploads

# Run the script
CMD ["python", "-u", "camera_ingestion.py"]