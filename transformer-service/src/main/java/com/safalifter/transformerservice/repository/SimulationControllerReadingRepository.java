package com.safalifter.transformerservice.repository;

import com.safalifter.transformerservice.entities.SimulationControllerReading;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SimulationControllerReadingRepository extends JpaRepository<SimulationControllerReading, Long> {
}
